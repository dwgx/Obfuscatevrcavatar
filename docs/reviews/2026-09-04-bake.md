# Dual-axis review — 2026-09-04 throwaway NDMF bake

Robot Avatar PC in `D:\Project\Unity\test`. Evidence: NDMF
`ManualProcessAvatar`, Unity console `[OVA]` lines, clone vs source dump in
`docs/THROWAWAY-BAKE.md` §7. `compile-check.ps1` exit 0 after the editor fix.
Agent did not SDK Publish.

## Spec axis

| Requirement | Result |
|---|---|
| Clone-only; source Hierarchy readable | **pass** — source still `Armature` `Body` `Dynamics` |
| Console four passes | **pass** — watermark / name / parameter / asset |
| Visemes stay usable | first bake **fail** (`v_aa` souped, descriptor still `v_aa`); after keep-list **pass** 15/15 |
| Armature / Body / Humanoid | **pass** |
| IEditorOnly gone on clone | **pass** |
| Watermark amplitude 1e-5 | **pass** — v0 delta ≈ 1e-5 |
| Not 100% anti-rip | mesh/tex *asset* names were readable; **souped on later bake** |
| No Kaguya / no F: unpack / no Publish | **pass** |

## Standard axis

| Check | Result |
|---|---|
| First viseme keep only `vrc.*` | SDK robot uses `v_sil` — lip sync would miss by name. Fixed. |
| Humanoid fallback by bone **name** | second `Chest` (collider) kept `Dynamics`. Fixed: skip names `GetBoneTransform` already found. |
| `FT` in `preserveNameSubstrings` | infix kept `*left` / `Gift`. **Fixed**: token-boundary (`FT_Blink` yes). |
| Test package vs product tree | VPM copy, not a junction. Fix copied into both. Listing zip not rebuilt. |

Criticals: none remaining for NDMF ManualProcess on this dummy after the
2026-09-04 viseme / Chest / FT-token / meshname iteration. Upload unpack and
AAO+TTT compose are the next cheap paths. Texture names needed
`Material.GetTexturePropertyNames` + `CopySerialized`; `Instantiate(Texture2D)`
kept the importer name.

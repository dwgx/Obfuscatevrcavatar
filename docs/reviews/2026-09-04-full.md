# Dual-axis independent review — OVA whole tree + 2026-09-04 arc

Reviewer: Grok product window, 2026-09-04. Read-only except this file and
HANDOFF Exact next action. Packet: `.agent/REVIEW-BRIEF.md`. Skills:
`code-review` + `fresh-review`. Implementer HANDOFF / dated REVIEW-* files
were leads only; claims below were re-run from disk.

Init (`session-probe` 1.9, this cwd):

```
cwd: D:\Project\Obfuscatevrcavatar
kind: other
workflow: CONTINUE-then-MAP
product_writes: allowed
git_root: none
doors: CLAUDE.md=yes AGENTS.md=yes origin.md=no LOOP.md=no CURRENT.md=no HANDOFF.md=yes
```

`kind: other` is the no-git tree, as expected. Owner named this review
deliverable. No `git init`. No SDK Publish. No Kaguya MCP calls (8080 down).

Classify: named-slice review. Workflow: `high-quality.md` + `coding-bar.md`.

---

## Findings (lead)

Rank: Critical blocks completion. Important must not be treated as done.
Normal/Minor do not flip the product identity.

### 1. Important — human SDK **upload** is not done

`%LOCALAPPDATA%\Unity\Editor\Editor.log` (mtime 2026-09-04 11:11, pid 9904):

- Line 808: `Two-factor authentication code for type: emailOtp verification failed: Bad Request`
- Line 828+: `Starting processing for avatar: Tutorial_Robot_Avatar_Dynamics_Demo_v1(Clone)` then `VRCSdkControlPanelAvatarBuilder/<Build>d__141` → NDMF preprocess
- Lines 2522–2537: TTT `Atlas Relocate Result` **Total relocate count: 19** (NDMF “Error Reported” is TTT `LogInfo`)
- Lines 3790–3793: AAO metrics `蒙皮网格数量: 2 → 1` (also logged via NDMF ErrorReport; it is **Info**, not a failed merge)
- Lines 4055–4238: `[OVA] watermark` → `decoy blendshapes added=8` → `name pass … decoy=8` → `parameter pass: renamed=2` → `asset name pass: clones=7` **after** AAO/TTT, on the SDK Build path
- Grep: **no** `Building Avatar`, `Uploading`, `.vrca`, `obft3st`, `Avatar Built`

Local NDMF on an SDK Build click **ran**. Publish / author-unpack of a live
`.vrca` did **not**. Watermark-after-VRC-compress stays **open**.

This session: `Unity.exe` not running. `127.0.0.1:8080` and `:17849`
`TcpTestSucceeded=False`. Live MCP identity not re-probed. Last log still
points ova-web at `D:\Project\Unity\test\Packages\dev.ova.protection\Editor\Web\wwwroot`.

### 2. Important — VPM listing zip 0.2.0 is not this working tree

`source.json` `zipSHA256` =
`a693233415659675d35367f796fbd42abe380f21f0af751db54507e4e3484dae`
matches `Releases/dev.ova.protection-0.2.0.zip` SHA256 (verified).

The zip is a **stale snapshot** of the same version number:

| Path | Zip | Disk |
|---|---|---|
| `Editor/OvaNameKeep.cs` | 9474, **no** `HasTokenBoundary` | 13464, token-boundary present |
| `Editor/OvaNameObfuscationPass.cs` | 6925 | 9347 (decoy path) |
| `Editor/OvaWatermarkPass.cs` | 2581 | 3465 (MeshFilter path) |
| `Runtime/OvaSettings.cs` | 2822 | 3566 |
| `Editor/Web/wwwroot/js/app.js` | 21971 | 27672 |
| `Editor/Web/wwwroot/js/i18n.js` | **missing** | 15085 |
| `Editor/Web/wwwroot/js/pack.js` | **missing** | 5078 |
| `Editor/Web/wwwroot/index.html` | 20546 | 27299 |

A VCC install from `source.json` does **not** ship viseme/`v_aa` keep, FT
token-boundary, decoys, labor-meshname, or the current ova-web catalog.
Do not treat “listing is 0.2.0” as “listing = HEAD”.

### 3. Important — MMD `Body` keep is not shown on the compose clone

`Temp/bake-w2.txt` / `bake-w3.txt` (identity `data=D:/Project/Unity/test/Assets`):

- `visemeMiss=0`
- `origSmr=2` `cloneSmr=1`
- clone mats share one souped 2048 tex
- `v_aa=False` `blink_left=False` `Ear-Left=False` **`BodyGo=False`**
- `cloneKids` = `Armature` + three soup names (no `Body`, no `Dynamics`, no `OVA_*`)
- clone SMR GameObject name is soup

`v_aa=False` is the driver’s **literal** blendshape string `v_aa`. Combined
with `visemeMiss=0` it means descriptor slots match the mesh; Robot visemes
are not necessarily the token `v_aa`. That is **not** a viseme miss.

`BodyGo=False` **is** a keep-list miss versus `docs/NAME-PASS.md` (“optional
MMD `Body` still hard-keep”). `FindMmdBody` keeps the first SMR with ≥4
reserved shapes, not a leftover GameObject still named `Body` after AAO
merge. §8 “Body kept” was the **pre-compose** iteration bake. §9–§10 do not
re-claim clone `Body`.

Robot is one Body mesh after merge. **Not** split-clothes proof.

### 4. Normal — ova-web “参数将改 0/8” ≠ bake `renamed=2`

`OvaSceneProbe` classifies **scene** animator + expression names.
`OvaParameterPass.Collect` walks NDMF `AnimatorServicesContext` **after**
MA/AAO. GUI smoke recorded overview 0/8; Wave 2/3 and SDK preprocess both
logged `renamed=2`. Preview is not bake. Do not tune keep-lists from the
overview chip alone.

### 5. Normal — decoys are per processed SMR, not per avatar

`RenameBlendShapes` adds `decoyWant` (clamped 0–32) **inside** the SMR loop.
After AAO, one mesh → `shapes` 23→31 and `[OVA] decoy blendshapes added=8`
(matches §10). Without merge, N meshes × N decoys; log total can exceed 32.
Settings comment reads as avatar-level.

### 6. Normal — TTT batchmode needs `TTTInitializeCaller.Initialize()`

Throwaway driver does this. Wave 2c dump: `tttInit=UStdHolder ColorFill=True`
`tttColorFillNre=False`. First batch without it NREd (documented in
`THROWAWAY-BAKE.md` §9). Not a runtime SDK issue once Editor `delayCall` has
run; it **is** a batch/CI footgun.

### 7. Normal — SyncedLayer / split-clothes / watermark-upload still unproven

`OvaAnimatorRewrite.MapParameters` walks `SyncedLayerMotionOverrides` +
BlendTree params. Robot dumps: `clipCurves=0`. No override fixture.

L7 stamp exists (SMR + MeshFilter, seed hash, ±amp). SDK preprocess logged
`meshes=1 amp=1E-05`. Survival after VRC compress: **open**.

### 8. Minor — live Unity MCP not available this session

No `Unity.exe`. Ports 8080 / 17849 closed. Last Editor identity was **test**
(ova-web root under `Unity\test\Packages\…`). Kaguya `Packages/dev.ova.protection`
**absent**.

`~\.cursor\mcp.json` has **no** Unity MCP (stop line holds for Cursor global).
`D:\Project\Unity\test\.cursor\mcp.json` has project-local `unityMCP` → `:8080`
(allowed). `~\.claude.json` still has a **Kaguya-keyed** `UnityMCP` at
`http://127.0.0.1:8080/mcp` — DCC residue, not an OVA dump this tree made.
If Kaguya Editor owns 8080 later, identity abort (`/test/` in `dataPath`)
remains mandatory.

### 9. Minor — NDMF “Error Reported: AAO / Atlas Relocate” is vendor Info

AAO 2→1 and TTT relocate 19 used `ErrorReport` for **metrics**. Merge and
atlas still happened (dumps + SDK log). Do not file as a failed compose.

---

## Spec axis — is this the asked product?

Must be present (GOAL + BRIEF):

| Requirement | Evidence this session | Verdict |
|---|---|---|
| Blue-team NDMF/VPM `dev.ova.protection` | package id, `ExportsPlugin`, no git remote | **pass** |
| ova-web `127.0.0.1:17849` | `OvaWebServer.Port = 17849`; last log bound it; port down now (Editor closed) | **pass** (code + last log) |
| L1 compose AAO/TTT, not reimplemented | plugin AfterPlugin AAO then TTT; dumps 2→1 SMR + shared 2048 tex | **pass** on Robot |
| L2 clone-only names/params/assets | source `origKids` readable; clone soup; `OvaProtection` IEditorOnly | **pass** |
| Keep-lists: viseme slots, Humanoid/Armature, GoGo, FT **token**, PhysBone prefix group | `OvaNameKeep` / `OvaPreserve`; dumps visemeMiss=0, Ear-Left/blink_left souped | **pass** with Body caveat (F3) |
| L7 tiny keyed watermark | `OvaWatermarkPass`; logs meshes=1 amp=1e-5 | **pass** in Editor; upload **open** |
| ova-web JSON `Library/OVA/settings.json` | test JSON seed 5145514 decoy=8 Expert zh-CN | **pass** |
| AfterPlugin MA / VF / AAO / TTT then OVA | `OvaProtectionPlugin.Configure` Optimizing; SDK log order watermark after AAO metrics | **pass** (also FaceEmo blink-disabler — extra empty constraint) |
| No in-avatar OSC unlock | no OSC wearer unlock in package; attest 501 stub | **pass** |
| Not installed in frozen Kaguya | no `kaguya/Packages/dev.ova.protection` | **pass** |
| Canonical UI host = package | content SHA256 host == product wwwroot (9 files) | **pass** |
| host = test copy | same 9 content hashes; test adds Unity `.meta` only | **pass** (content) |
| Throwaway driver not in VPM | `Assets/OVA/Editor/OvaThrowawayBake.cs` only; zip has no `Assets/OVA` | **pass** |
| test package not a junction | `LinkType` empty; UPM `file:dev.ova.protection` embedded | **pass** |
| test C# == product C# | 31 content files hash-equal | **pass** |
| Vendor OSC/vertex-lock notes 1p | URLs fetched this session (below) | **pass** for cited HTTP; Ajisai still “not re-audited”; SaoMoLa table is local README |

Must **not** appear:

| Forbidden | Evidence | Verdict |
|---|---|---|
| SaoMoLa harvest / decrypt / key scan / drivers | package `*.cs` has none of those; stop docs present | **pass** (this repo) |
| lilToon vertex lock | plugin logs compose-only; no unofficial lock shaders | **pass** |
| `_BitKey` / `DexProtect` / `_IsLocked` in package `*.cs` | grep **no matches** (product + test copy) | **pass** |
| Agent SDK Publish | this window did not; log shows human Build preprocess only | **pass** |
| Unity MCP in `~\.cursor\mcp.json` | no unity/8080 there | **pass** |
| 100% anti-rip marketing | README “100% 防不住”; GOAL same | **pass** |
| `git init` / public remote | `.git` absent | **pass** |

Spec extras that are **not** the asked product (do not treat as missing):
attest Hop B parked (501 `publish-not-wired`); ShellProtector not on Robot.

**Spec criticals: none** against the blue-team plugin identity.
**Spec blockers to close the 2026-09-04 arc:** F1 upload, F2 listing zip, F3
MMD Body after AAO, watermark-after-compress.

---

## Standard axis — correctness / safety

| Check | Result |
|---|---|
| `scripts/compile-check.ps1` | exit 0; runtime dll + editor dll under `Temp/compile/` |
| Clone-only | passes Instantiate + AssetSaver; dumps source names intact | ok |
| Viseme after AAO rename | `visemeMiss=0` on w2/w3; SDK preprocess used same decoy=8 path | ok |
| FT token-boundary | code `HasTokenBoundary`; dumps `blink_left=False` `Ear-Left=False` | ok on Robot |
| Second Humanoid `Chest` | fallback skips names already in `GetBoneTransform`; `Dynamics` not kept as plaintext on clone | ok on Robot |
| Mesh/clip/tex asset names souped | dumps clone mesh/tex soup; `clipNames=6` | ok on Robot |
| Decoy names vs keep-list | `AppendDecoyShapes` uses `Names.Next()` after keep pass | ok on paper; per-mesh (F5) |
| PhysBone prefix group | `IsPhysBoneName` prefix + `prefix_*` | code-ok; Robot not a grab fixture |
| Secrets | `OvaAttest` PAT only in `Library/OVA/secrets.json`; status booleans | ok |
| HttpListener | ova-web main-thread queue (prior review); not re-browsered (port down) | **not re-verified UI** |
| TTT ColorFill NRE | gated in throwaway driver | ok after init |
| execute_code missing | GUI smoke; this session could not rebake via MCP anyway (8080 down) | tool gap |
| Robot limits | no split clothes; no SyncedLayer override; viseme token `v_aa` not on mesh | documented |
| Listing vs HEAD | F2 | **fail** for “ship 0.2.0 = current” |
| Kaguya freeze | package not there | ok |

**Standard criticals: none.**
**Standard importants:** listing zip drift (F2), MMD Body after merge (F3),
preview vs bake params (F4).

Security defaults (guilty until negative case): lock fingerprints grepped
absent in `*.cs`; pack.js only **mentions** them in a “never emit” comment
(not a runtime key). No OSC unlock path in Editor C#.

---

## Vendor citations (re-opened)

| Citation | Opened this session | Matches VENDOR-OSC-LOCKS.md |
|---|---|---|
| https://github.com/PlagueVRC/AntiRip | yes (README: 32-bit params, no lilToon in supported table, not foolproof, specialized hotswap in-game not Unity) | yes |
| https://raw.githubusercontent.com/PlagueVRC/AntiRip/master/Readme/FAQ.md | yes (Quest unsupported; Quest twin negates; cannot delist ripper store) | yes |
| https://jinxxy.com/Dextro/DexProtect | yes (OSC app, 19 bits, Custom Animations, keys under Documents/DexProtect, “no guarantee… not impossible”) | yes |
| https://share.lumina.moe/posts/vrchat-anti-ripper/ | yes (hedgehog; merge+atlas 精神攻击; OSC/password weak) | yes |
| `vrchat-il2cpp-re/…/avatar_ripping_defense_playbook.md` §0 | local file present; plaintext-geometry law | yes |
| SaoMoLa detector table | local `D:\Project\SaoMoLa\README.md` `_BitKey` / `DexProtect` / `_IsLocked` | yes (no HTTP URL in VENDOR; **detector** grade) |
| AjisaiFlow 0.50.0 | not re-fetched; VENDOR already says unaudited | still weak |

Do not copy SaoMoLa scripts. Classes only — this review did not.

---

## Commands actually run (this window)

```
powershell -NoProfile -File C:\Users\dwgx1\.agent-system\scripts\session-probe.ps1
powershell -NoProfile -File scripts\compile-check.ps1
  → OK runtime=Temp\compile\dev.ova.protection.dll
     OK editor=Temp\compile\dev.ova.protection.editor.dll
rg _BitKey|DexProtect|_IsLocked  Packages/dev.ova.protection  *.cs  → no matches
rg same on D:\Project\Unity\test\Packages\dev.ova.protection *.cs → no matches
SHA256 content wwwroot host == pkg == test (9 files; test extra .meta only)
tree hash host=pkg 172799C0… ; test tree differs only by .meta
Get-FileHash Releases/dev.ova.protection-0.2.0.zip → A6932334… = source.json
zip entry sizes vs disk (table in F2); zip OvaNameKeep HasTokenBoundary=False
dir /AL + Get-Item LinkType on test Packages\dev.ova.protection → not a junction
product vs test {cs,json,asmdef,html,js,css,svg} → 31 equal, 0 mismatch
Test-Path kaguya\Packages\dev.ova.protection → False
Test-Path Obfuscatevrcavatar\.git → False
Get-CimInstance Unity.exe → none; Hub leftover -projectPath test
Test-NetConnection 127.0.0.1:8080 / 17849 → False
Select-String Editor.log / Editor-prev.log ([OVA], AAO 2→1, Relocate 19, emailOtp)
web_fetch AntiRip README + FAQ + Jinxxy DexProtect + Lumina
```

**Not run:** live Unity MCP `dataPath`; Chrome ova-web; NDMF rebake; SDK
Publish; author-unpack; `pack-vpm.ps1`.

---

## Ruling

**blocked** — do not close the 2026-09-04 throwaway / SDK / docs arc, and do
not treat local VPM `0.2.0` as the working tree.

The asked **plugin** is present: clone-only L2, compose L1 on Robot, keyed L7
in Editor, ova-web JSON, keep-lists (viseme slots + FT token-boundary), no
lock fingerprints, no Kaguya install, no SaoMoLa port, no agent Publish.
No spec or standard **criticals** against that identity.

Blocked means: human upload + author-unpack **open**; listing zip **stale**;
MMD `Body` name after AAO **not shown**; watermark-after-compress **open**;
SyncedLayer / split-clothes **unproven**. Implementer HANDOFF already listed
several of these as open — this review **agrees** and does not upgrade them.

Repair is a **separate** Owner word (`repair`). This window did not patch.

Exact next action lives in `.agent/HANDOFF.md`.

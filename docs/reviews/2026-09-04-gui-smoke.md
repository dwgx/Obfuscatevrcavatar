# Dual-axis review — 2026-09-04 GUI Unity MCP + ova-web smoke

Reviewer is this Cursor session against a **live** `D:\Project\Unity\test`
Editor (pid 9904, 2022.3.22f1). Home chat has no `unityMCP` namespace; HTTP
was `Temp/mcp-call.py` → `http://127.0.0.1:8080/mcp`. Agent did not SDK
Publish. Kaguya Editor was not running. OVA is not in the Kaguya Packages
tree.

Classify: **bounded** review. Read-only except `OVA/打开 ova-web` (existing
menu) so `/api/health` could be checked.

## Evidence split

| Claim | This GUI session | Prior batch dumps |
|---|---|---|
| Identity `D:/Project/Unity/test` | **live** MCP `debug_request_context` cwd + Unity `-projectPath` | — |
| Scene `Assets/OVA/RobotBake.unity` | **live** `manage_scene` | — |
| `OvaProtection` on robot root | **live** hierarchy `componentTypes` | — |
| `OVA_AAO_Merge` / `OVA_TTT_Atlas` children | **live** `find_gameobjects` by_path | — |
| Packages OVA 0.2.0 / AAO 1.9.18 / TTT 1.0.2 | **live** `manage_packages` | — |
| ova-web seed 5145514, decoy=8, Expert zh-CN | **live** `/api/settings` + Chrome Protect tab | — |
| visemeMiss=0, SMR 2→1, shared atlas tex, decoy +8 | **not re-run** | `Temp/bake-w2.txt` / `bake-w3.txt` |
| Console `[OVA] decoy blendshapes added=` | this Editor: ova-web start only | batch log |

`execute_code` is in group `scripting_ext` (enabled) but **not** in
`tools/list` (47 tools). Direct `tools/call` returned `{success:false}` with
no message. This session therefore **cannot** NDMF `ManualProcessAvatar` from
MCP. Wave 2/3 clone metrics stay the earlier batchmode dumps.

## Spec axis

Mission: Waves 0–3 on throwaway `test`; never Kaguya / never agent Publish /
never cache intercept; clone-only names + compose AAO/TTT; decoy blendshapes.

| Requirement | Live | Verdict |
|---|---|---|
| Wave 0 identity is `test`, not Kaguya | Unity cmdline, MCP cwd, plugin `test@a3d966f6667eb787`, 8080 pidfile under `test\Library\MCPForUnity` | **pass** |
| RobotBake + OVA marker | root `Tutorial_Robot_Avatar_Dynamics_Demo_v1` has `OvaProtection` + `VRCAvatarDescriptor` | **pass** |
| Wave 2 compose objects on source | `OVA_AAO_Merge` (MergeSkinnedMesh), `OVA_TTT_Atlas` (AtlasTexture); live types exist | **pass** (setup) |
| Wave 3 decoy setting | JSON + ova-web Expert field **8**; clamp 0–32 in store | **pass** (config) |
| AfterPlugin AAO then TTT then OVA | `OvaProtectionPlugin.Configure` Optimizing | **pass** (code) |
| Clone-only / no lock fingerprints | no `_BitKey` / `DexProtect` / `_IsLocked` in `*.cs`; pack.js forbids them | **pass** |
| Test package not a junction | `dir /AL Packages` empty; UPM source Embedded at `test\Packages\dev.ova.protection` | **pass** |
| Throwaway driver not in VPM | `Assets/OVA/Editor/OvaThrowawayBake.cs` only | **pass** |
| ova-web from Editor, not preview.ps1 | `/api/health` `unity:true`; wwwroot = test package copy | **pass** |
| wwwroot host = prod = test | SHA256 match `index.html` / `app.js` / `pack.js` / `ova.css` | **pass** |
| Wave 1–3 clone keep/soup/atlas | not observed in this Editor | **open** — do not treat batch dumps as this GUI bake |
| Human SDK Publish | not done | **open** (Owner) |

No spec criticals for the **setup** the GUI actually shows.

## Standard axis

| Check | Result |
|---|---|
| compile-check.ps1 | exit 0 (runtime + editor) |
| Unity MCP not in `~\.cursor\mcp.json` | pass (project `.cursor/mcp.json` HTTP only) |
| Console errors this session | `read_console` errors = 0 |
| NDMF Preview scene loaded | empty `___NDMF Preview___` (rootCount 0) — plugin present, not a bake |
| Source Hierarchy readable | robot childCount 5; merge/atlas/Dynamics found by path; Armature/Body path search returns prefab-negative IDs (MCP quirk, not soup) |
| SMR count on source | `SkinnedMeshRenderer` ×2 (matches Wave 2 `origSmr=2` before clone) |
| Decoys vs viseme | `AppendDecoyShapes` uses `Names.Next()` soup names; keep-list runs first | ok on paper |
| Per-mesh decoys | `decoyWant` applied to **each** processed mesh. After AAO merge that is 1×8. Without merge it would be N×8 | Normal nit |
| Overview vs last bake | GUI “参数名 将改 **0** / 8”; batch `parameter pass: renamed=2` | Normal nit — preview ≠ bake |
| GUI `execute_code` | missing from tool list; cannot live-rebake | tool gap, not product |

Criticals: **none** for “is the throwaway Editor the right project with OVA+AAO+TTT+ova-web wired.”

## Ruling

**accepted** for Wave 0 identity + scene/package/ova-web wiring on the live
GUI. Wave 2/3 **clone** claims remain the 2026-09-04 batch dumps
(`docs/THROWAWAY-BAKE.md` §9–§10), not re-proven in this Editor.

To GUI-rebake: turn on CoplayDev **execute_code** (scripting_ext already
enabled) or run `OvaThrowaway.OvaThrowawayBake.Wave2And3` in batchmode.
Do not SDK Publish from the agent.

## Open (unchanged)

- Human SDK upload + author-unpack
- Listing zip vs working tree
- Watermark upload survival
- SyncedLayer overrides; split-clothes proof (Robot is one Body)
- Attest Hop B parked

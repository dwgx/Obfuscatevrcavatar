# Dual-axis review — 2026-09-03 ova-web logic/layout slice

Later audit (engine + HTTP, 2026-09-04): `.agent/REVIEW-2026-09-04-r1-r2.md`.
Full arc + fingerprint HTTP (2026-09-04): `.agent/REVIEW-2026-09-04-arc.md`.
Throwaway NDMF bake (2026-09-04): `.agent/REVIEW-2026-09-04-bake.md`.
GUI Unity MCP + ova-web smoke (2026-09-04): `.agent/REVIEW-2026-09-04-gui-smoke.md`.
Full independent review packet: `.agent/REVIEW-BRIEF.md` (next window writes `REVIEW-2026-09-04-full.md`).
This file’s **accepted** ruling for the UI/logic slice is unchanged.

---

# Dual-axis review — 2026-09-03 ova-web logic/layout slice

Reviewer is a later pass on the same machine, against current disk, not the
implementer's chat. Evidence: `scripts/compile-check.ps1` exit 0; Chrome
DevTools on `http://127.0.0.1:17849/` (preview); SHA256 match of
`host/ova-web/wwwroot/{index.html,css/ova.css,js/app.js}` vs package copy.

## Spec axis

Owner ask: keep expanding engine logic and ova-web layout/features (not empty
pages).

| Requirement | Disk | Verdict |
|---|---|---|
| Param picker + exact pins | `pinPreserve`, `#param-list`, chips | present |
| Substring chips | protect + params, two CSVs | present |
| Layout shows live effect | overview `将改 n / total`, protect name-meta | present |
| Engine matches UI | name substrings no longer applied to params; name pass reads them | present |
| Animator curves / synced layers | `OvaAnimatorRewrite` | present (paper; bake still open) |
| Visual language locked | brass/paper, custom SVG, no Material Symbols in HTML | present; GOAL/README still said Symbols — patched this review |

Extra (not asked, small): Ctrl+S, beforeunload, `/api/scene` alias, PhysBone
rows in the picker.

Ungrilled product fork: PhysBone **prefix + `prefix_*` kept as a group**.
Documented in `CLAUDE.md` / `docs/NAME-PASS.md`. Owner may override.

## Standard axis

| Check | Result |
|---|---|
| Clone-only | passes still Instantiate / AssetSaver; scene assets not renamed | ok |
| HttpListener threading | scene probe on main-thread queue | ok |
| Secrets | PAT not in settings JSON; status returns booleans | ok |
| Humanoid / Armature / viseme | `OvaNameKeep.BuildStructural` + reserved shapes | ok |
| wwwroot drift | hashes matched at review time | ok |
| compile-check | green | ok |

### Findings

1. **Normal — `FT` auto-hint is wide.** `OvaPreserve.AutoHints` uses substring
   `FT`, so `Gift` / `Softness` stay. Known; do not "fix" without Owner (GoGo /
   face-tracking safety).
2. **Normal — preview settings were dirty.** Hierarchy off + sandbox attest +
   pinned `MyToggle` in `Temp/ova-web-preview/settings.json`. Reset this review.
3. **Minor — `OvaPreserve.Build` still inserts hint tokens into the exact
   HashSet.** Keep is still decided by `IndexOf` / prefix / PhysBone helpers.
   Harmless; do not drive-by-clean unless touching that file.
4. **Open — no throwaway bake.** Parameter curves, watermark-after-second-clone,
   and PhysBone prefix survival are **observed** in code, not **verified** on an
   avatar.

No criticals for the asked slice.

## Ruling

**accepted** for logic + ova-web layout. Next product proof is Owner bake or
attest publish, not more chrome.

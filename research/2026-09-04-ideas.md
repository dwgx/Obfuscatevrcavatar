# GPT ideas vs OVA — 2026-09-04

GPT stayed inside the pack stop lines. Independent review `blocked` is treated as correct. Path B remains residual, not a product failure.

Owner-level line from GPT (accepted as a **proposal**, not a frozen GOAL rewrite until Owner says so):

> OVA should be a one-way build compiler + local attribution evidence system, not another runtime unlock pack.

## Dual-axis on the reply

Spec: restates Path A/B, three keys, no Hop B as viewer gate, no lilToon vertex lock, no Kaguya, no agent Publish. Rejects “hide platform AES”, HTTP-then-mesh, in-memory scramble, and “detector miss = safe.”

Standard: idea cards have test_failure. Effort L items (watermark-v2) are not mixed into the default knife. Shell/Ajisai stay optional compose.

Shrink before coding:

- **idea-06** (watermark-v2 ECC + secret + spatial canonicalize) is a research program, not one slice. Do not start it before **idea-07** (one human Publish) answers whether ±1e-5 even survives.
- **idea-01** fixture is the right *method*, but it lives in `D:\Project\Unity\test`, needs a dummy with real split meshes. Without that asset, it stalls.
- **idea-02** Body-anchor is the smallest **code** gap already evidenced (`BodyGo=False`).
- **idea-12** zip parity is the smallest **shipping** gap. Independent mission.

## Ranking (agent, not Owner ruling)

| Priority | Id | When |
|---|---|---|
| 1 | idea-01 fixture | Owner can drop a throwaway multi-mesh dummy into `test` (not Kaguya) |
| 2 | idea-02 Body-anchor | Owner wants a code slice **now**, no new avatar |
| 3 | idea-12 pack-vpm | Owner says **repair** and cares about VCC zip |
| 4 | idea-07 upload calibration | Owner says they will click SDK Publish |
| later | 03 decoy budget, 04 audit, 05 surface report, 11 build-report | after 01 or 02 |
| later | 06 watermark-v2 | only after 07 |
| later | 09 Shell on test | Owner accepts OSC author-key compose |
| park | 10 Ajisai | after OVA blockers |
| reject as product | 13 strong no-key vertex lock | GPT already rejected; L1.5 puzzle not next |

Three mutually exclusive next windows (GPT E). Do not run two.
